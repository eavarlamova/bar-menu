const initialState = {

}

const ingredientsReducers = (state = initialState, {type, payload}) => {
    switch(type){
        default:
            return {...state}
    }
};

export default ingredientsReducers;
